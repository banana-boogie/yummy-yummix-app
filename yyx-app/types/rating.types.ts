// Admin feedback view with joined data
export interface AdminFeedbackItem {
    id: string;
    feedback: string;
    createdAt: string;
    recipeName: string;
    recipeId: string;
    userEmail: string;
    userId: string;
}
