/** Full locale string for data/API calls (e.g. 'es-MX', 'en', 'es') */
export type Locale = string;

/** Language code used by i18n UI strings — limited to the two bundles we ship */
export type Language = 'en' | 'es';
