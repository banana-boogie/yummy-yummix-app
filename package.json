{
  "name": "yummy-yummix-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "YummyYummix monorepo - React Native app and Supabase Edge Functions",
  "scripts": {
    "prepare": "husky",
    "verify-hooks": "test -x .husky/pre-commit && echo '✅ Git hooks are installed' || (echo '❌ Git hooks not installed. Run: npm install' && exit 1)",
    "precommit": ".husky/pre-commit",
    "agents:sync": "node scripts/agents-sync.js",
    "agents:check": "node scripts/agents-sync.js --check"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "js-yaml": "^4.1.1"
  }
}
