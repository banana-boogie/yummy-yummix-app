#!/bin/sh

# Exit on any error
set -e

# Get list of staged files
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM)

if [ -z "$STAGED_FILES" ]; then
  echo "No staged files, skipping pre-commit checks"
  exit 0
fi

# Block committing local environment files
SENSITIVE_ENV_FILES=$(echo "$STAGED_FILES" | grep -E '(^|/)\.env($|\.local$|\.development$|\.production$|\.test$)' | grep -v '\.example$' || true)
if [ -n "$SENSITIVE_ENV_FILES" ]; then
  echo "❌ Refusing to commit local environment files:"
  echo "$SENSITIVE_ENV_FILES"
  echo "Use .env.example templates instead."
  exit 1
fi

# Block obvious secret leaks in added lines
SENSITIVE_ADDITIONS=$(git diff --cached --unified=0 | grep -E '^\+.*(SUPABASE_SERVICE_ROLE_KEY=.*eyJ|sk-proj-[A-Za-z0-9_-]{20,}|-----BEGIN [A-Z ]*PRIVATE KEY-----)' | grep -v '^+SENSITIVE_ADDITIONS=' || true)
if [ -n "$SENSITIVE_ADDITIONS" ]; then
  echo "❌ Potential secret detected in staged changes. Remove secrets before commit."
  exit 1
fi

# Syntax-check staged shell scripts
YYX_SERVER_SHELL_FILES=$(echo "$STAGED_FILES" | grep '^yyx-server/scripts/.*\.sh$' || true)
if [ -n "$YYX_SERVER_SHELL_FILES" ]; then
  echo "🔍 Running shell syntax checks..."
  while IFS= read -r file; do
    bash -n "$file"
  done <<EOF
$YYX_SERVER_SHELL_FILES
EOF
  echo "✅ Shell syntax check passed"
fi

# ============================================================
# YYX-APP: ESLint via lint-staged
# ============================================================
YYX_APP_FILES=$(echo "$STAGED_FILES" | grep "^yyx-app/" || true)
if [ -n "$YYX_APP_FILES" ]; then
  echo "🔍 Running lint-staged for yyx-app..."
  cd yyx-app && npx lint-staged
  cd ..
fi

# ============================================================
# YYX-SERVER: Deno format and lint
# ============================================================
YYX_SERVER_FILES=$(echo "$STAGED_FILES" | grep "^yyx-server/supabase/functions/" || true)
if [ -n "$YYX_SERVER_FILES" ]; then
  echo "🔍 Checking deno fmt for yyx-server..."
  cd yyx-server

  # First check if formatting is needed
  if ! deno task fmt:check > /dev/null 2>&1; then
    echo "⚠️  Formatting issues detected. Auto-formatting..."
    deno task fmt

    # Re-add the formatted files
    cd ..
    echo "$YYX_SERVER_FILES" | while read -r file; do
      if [ -f "$file" ]; then
        git add "$file"
      fi
    done
    cd yyx-server

    echo "✅ Files formatted and re-staged"
  else
    echo "✅ Formatting check passed"
  fi

  echo "🔍 Running deno lint for yyx-server..."
  # Only lint staged .ts files, using deno.json config for rule exclusions
  LINT_FILES=$(echo "$YYX_SERVER_FILES" | grep '\.ts$' | sed 's|^yyx-server/||' || true)
  if [ -n "$LINT_FILES" ]; then
    echo "$LINT_FILES" | xargs deno lint --config deno.json
  fi
  echo "✅ Lint check passed"

  cd ..
fi

echo ""
echo "✅ All pre-commit checks passed!"
